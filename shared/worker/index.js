/**
 * Worker Module - Entry point for distributed worker components
 * 
 * Exports the WorkerNode class and related utilities for distributed processing.
 */

const WorkerNode = require('./WorkerNode');

module.exports = {
  WorkerNode
};