# Telegram Bot Token
# Get this from BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Target Login URL
# The URL of the page you want to automate credential checking for
TARGET_LOGIN_URL=https://example.com/login

# (Optional) Timeout in milliseconds for Puppeteer operations
# Default: 60000 (60 seconds)
TIMEOUT_MS=60000

# (Optional) Proxy server URL if you need to route through a proxy
# Example: http://proxy.example.com:8080
PROXY_SERVER=

# (Optional) Always take screenshots on credential checks
# Default: false (only on errors)
SCREENSHOT_ON=false

# (Optional) Custom user agent for Puppeteer
# If not set, uses a default desktop Chrome user agent
# USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36...
