# Rakuten Credential Checker Bot

# Required: Telegram Bot Token from @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Required: Rakuten OAuth login URL
TARGET_LOGIN_URL=your_rakuten_oauth_url_here

# Optional: Allowed Telegram user IDs (comma-separated). If not set, all users can use the bot.
# Get your user ID by messaging @userinfobot on Telegram
# ALLOWED_USER_IDS=123456789,987654321

# Optional: Request timeout in milliseconds (default: 60000)
TIMEOUT_MS=60000

# Optional: Parallel batch checks (default: 3)
BATCH_CONCURRENCY=3

# Optional: Delay between batch chunks in ms (default: 50)
BATCH_DELAY_MS=50

# Optional: Human delay multiplier for batch mode (default: 0 = disabled for speed)
# Set to 0.1 for 10% of normal delays, 1 for full delays
BATCH_HUMAN_DELAY_MS=0

# Optional: Logging level - error|warn|info|debug (default: info)
LOG_LEVEL=info

# Optional: Proxy server URL (supports any format: host:port, user:pass@host:port, http://...)
# PROXY_SERVER=http://user:pass@host:port

# Optional: Processed credentials cache TTL in ms (default: 7 days)
# PROCESSED_TTL_MS=604800000

# Optional: Redis URL for cloud deployment (uses JSONL locally if not set)
# REDIS_URL=redis://default:password@host:port
